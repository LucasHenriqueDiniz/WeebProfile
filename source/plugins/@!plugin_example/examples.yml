- name: 🦀 LastFM 🦀
  uses: LucasHenriqueDiniz/WeebProfile@main
  with:
    FILENAME: Example.svg
    GIST_ID: ${{ secrets.GIST_ID }}
    GH_TOKEN: ${{ secrets.GH_TOKEN }}
    STORAGE_METHOD: gist
    SIZE: half

    PLUGIN_EXAMPLE: true
    PLUGIN_EXAMPLE_HIDE_HEADER: true
    PLUGIN_EXAMPLE_STYLE: default
