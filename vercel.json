{
  "version": 2,
  "buildCommand": "pnpm build:dashboard",
  "outputDirectory": "weeb-dashboard/.next",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./weeb-dashboard"
}
