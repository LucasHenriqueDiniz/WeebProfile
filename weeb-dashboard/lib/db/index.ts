/**
 * Legacy PostgreSQL db export — only kept for compatibility with scripts.
 * Pages Functions use getDb(env) from functions/api/_shared/db.ts instead.
 *
 * TODO(phase-2): remove once all scripts are updated or deleted.
 */
export { schema } from "./schema-exports"
