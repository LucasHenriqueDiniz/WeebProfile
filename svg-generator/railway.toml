# Railway configuration for svg-generator
# Railway will automatically detect this file

[build]
builder = "nixpacks"

[deploy]
startCommand = "cd svg-generator && pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10


