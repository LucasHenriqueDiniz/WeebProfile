function to check source\plugins

check if they have all the assets images for each section.
check if the have a ENV_VARIABLE
check if they have a examples.yml (maybe not, is this really useful?)
check if they have a README
check if they have tests folder for generating dummy data
check if the have a Render[pluginName].tsx, etc (try to make all the names consistent)

then run prettier and slint